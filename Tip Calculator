print("Welcome to the tip calculator")
totall = input("How much was the total bill? £")
tipp = input("What percentage tip would you like to give? ")
peoplee = input("How many people to split the bill? ")
total=float(totall)
tip=float(tipp)
people = float(peoplee)
ttip=((tip/100)+1)
sum = ((total/people)*ttip)
ssum = "{:.2f}".format(sum)
print(f"Each person should pay £{ssum}")
